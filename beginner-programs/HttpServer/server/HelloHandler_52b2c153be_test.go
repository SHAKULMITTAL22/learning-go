// Test generated by RoostGPT for test roost-test using AI Type Vertex AI and AI Model code-bison-32k

 package server

import (
	"net/http"
	"testing"

	"github.com/gorilla/mux"
)

func TestHelloHandler_52b2c153be(t *testing.T) {
	// Table driven tests with test description
	tests := []struct {
		name string
		desc string
		want string
	}{
		{"John", "Valid name provided", "Hello, John"},
		{"", "Empty name provided", "Hello, world"},
		{"123", "Numeric name provided", "Hello, 123"},
		{"<script>", "Special character provided", "Hello, &lt;script&gt;"},
	}

	for _, tt := range tests {
		t.Run(tt.desc, func(t *testing.T) {
			// Create a new request with the given name
			req, err := http.NewRequest("GET", "/hello/"+tt.name, nil)
			if err != nil {
				t.Fatal(err)
			}

			// Create a new response recorder to capture the response
			rec := httptest.NewRecorder()

			// Create a new router and add the HelloHandler to it
			router := mux.NewRouter()
			router.HandleFunc("/hello/{name}", HelloHandler)

			// Serve the request through the router
			router.ServeHTTP(rec, req)

			// Check the response status code
			if rec.Code != http.StatusOK {
				t.Errorf("Expected status code %d, got %d", http.StatusOK, rec.Code)
			}

			// Check the response body
			if rec.Body.String() != tt.want {
				t.Errorf("Expected body %q, got %q", tt.want, rec.Body.String())
			}
		})
	}
}