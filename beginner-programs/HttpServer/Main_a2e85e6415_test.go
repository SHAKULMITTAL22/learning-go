// Test generated by RoostGPT for test roost-test using AI Type Vertex AI and AI Model code-bison-32k

 package main

import (
    "github.com/tannergabriel/learning-go/beginner-programs/HttpServer/server"
    "log"
    "net/http"
    "testing"
)

func TestMain_a2e85e6415(t *testing.T) {
    tests := []struct {
        name string
        port string
        want string
    }{
        {
            name: "success",
            port: "8080",
            want: "Listening on http://localhost:8080/",
        },
        {
            name: "failure",
            port: "8081",
            want: "ListenAndServe: bind: address already in use",
        },
    }
    for _, tt := range tests {
        t.Run(tt.name, func(t *testing.T) {
            server.NewServer()
            out, err := os.Stdout.WriteString(tt.want)
            if err != nil {
                t.Error(err)
            }
            if out != tt.want {
                t.Errorf("got %s, want %s", out, tt.want)
            }
        })
    }
}