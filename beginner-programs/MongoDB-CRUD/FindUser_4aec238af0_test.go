// Test generated by RoostGPT for test roost-test using AI Type Vertex AI and AI Model code-bison-32k

package main

import (
	"context"
	"fmt"
	"log"
	"testing"

	"go.mongodb.org/mongo-driver/bson"
	"go.mongodb.org/mongo-driver/mongo"
	"go.mongodb.org/mongo-driver/mongo/options"
)

type User struct {
	Name string
	Age  int
}

func findUser(collection *mongo.Collection, userName string) User {
	filter := bson.D{{"name", userName}}

	var result User

	err := collection.FindOne(context.TODO(), filter).Decode(&result)
	if err != nil {
		log.Fatal(err)
	}

	fmt.Printf("Found a single document: %+v\n", result)

	return result
}

func TestFindUser_4aec238af0(t *testing.T) {
	// Setup
	client, err := mongo.Connect(context.TODO(), options.Client().ApplyURI("mongodb://localhost:27017"))
	if err != nil {
		t.Error(err)
	}

	collection := client.Database("test").Collection("users")

	// Insert a document into the collection
	_, err = collection.InsertOne(context.TODO(), User{Name: "John Doe", Age: 30})
	if err != nil {
		t.Error(err)
	}

	// Call the function under test
	user := findUser(collection, "John Doe")

	// Assertions
	if user.Name != "John Doe" {
		t.Errorf("Expected name to be 'John Doe', got '%s'", user.Name)
	}

	if user.Age != 30 {
		t.Errorf("Expected age to be 30, got %d", user.Age)
	}

	// Teardown
	err = client.Disconnect(context.TODO())
	if err != nil {
		t.Error(err)
	}
}
