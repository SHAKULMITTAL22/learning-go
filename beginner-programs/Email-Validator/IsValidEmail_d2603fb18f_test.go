// Test generated by RoostGPT for test roost-test using AI Type Azure Open AI and AI Model roost-gpt4-32k

package validator

import (
	"testing"
)

// TestIsValidEmail_d2603fb18f tests the IsValidEmail function.
func TestIsValidEmail_d2603fb18f(t *testing.T) {
	var tests = []struct {
		input string
		want  bool
	}{
		// test cases for valid email addresses
		{"test@example.com", true},
		{"john.doe@example.co.uk", true},

		// test cases for invalid email addresses
		{"test", false},
		{"", false},

		// test case for email addresses with length more than 254
		{"this.is.a.very.long.email.address.email.address.email.address.email.address.email.address.email.address.email.address.email.address.email.address.email.address.email.address.email.address.email.address.email.address@email.com", false},
	}

	for _, test := range tests {
		t.Run(test.input, func(t *testing.T) {
			got := IsValidEmail(test.input)

			t.Log("Input: ", test.input, "Expected: ", test.want, "Got: ", got) // logging test data

			if got != test.want {
				// logging the error details
				t.Error("For: ", test.input, "Expected: ", test.want, "Got: ", got)
			} else {
				t.Log("Success for: ", test.input) // logging success message
			}
		})
	}
}
