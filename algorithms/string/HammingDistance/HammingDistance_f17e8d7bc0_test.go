// Test generated by RoostGPT for test math-go using AI Type Open AI and AI Model gpt-4

package HammingDistance

import (
	"log"
	"testing"
)

func calculateHammingDistance(a, b string) int {
	if len(a) != len(b) {
		log.Fatal("Strings are of different length")
	}

	var distance int = 0

	for i := 0; i < len(a); i++ {
		if a[i] != b[i] {
			distance++
		}
	}

	return distance
}

func TestHammingDistance_f17e8d7bc0(t *testing.T) {
	t.Run("Test with equal length strings", func(t *testing.T) {
		a := "abcd"
		b := "abce"
		expected := 1

		result := calculateHammingDistance(a, b)

		if result != expected {
			t.Errorf("Failed! Expected %d but got %d", expected, result)
		} else {
			t.Logf("Success! Expected %d and got %d", expected, result)
		}
	})

	t.Run("Test with different length strings", func(t *testing.T) {
		a := "abcd"
		b := "abcde"
		defer func() {
			if r := recover(); r == nil {
				t.Error("The function did not panic but it should have")
			}
		}()

		calculateHammingDistance(a, b)
	})
}
