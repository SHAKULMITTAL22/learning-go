// Test generated by RoostGPT for test math-go using AI Type Open AI and AI Model gpt-4

package IsPowerOfTwo

import (
	"testing"
)

func TestModFunction_10abfc4edf(t *testing.T) {
	t.Run("Test case 1: Positive numbers", func(t *testing.T) {
		a, b := 10, 3
		got := modFunction(a, b)
		want := 1
		if got != want {
			t.Errorf("modFunction(%d, %d) = %d; want %d", a, b, got, want)
		} else {
			t.Logf("modFunction(%d, %d) = %d; test success", a, b, got)
		}
	})

	t.Run("Test case 2: Negative numbers", func(t *testing.T) {
		a, b := -10, -3
		got := modFunction(a, b)
		want := -1
		if got != want {
			t.Errorf("modFunction(%d, %d) = %d; want %d", a, b, got, want)
		} else {
			t.Logf("modFunction(%d, %d) = %d; test success", a, b, got)
		}
	})

	t.Run("Test case 3: Negative and positive numbers", func(t *testing.T) {
		a, b := -10, 3
		got := modFunction(a, b)
		want := 2
		if got != want {
			t.Errorf("modFunction(%d, %d) = %d; want %d", a, b, got, want)
		} else {
			t.Logf("modFunction(%d, %d) = %d; test success", a, b, got)
		}
	})
}

func modFunction(a, b int) int {
	m := a % b
	if a < 0 && b < 0 {
		m -= b
	}
	if a < 0 && b > 0 {
		m += b
	}
	return m
}
