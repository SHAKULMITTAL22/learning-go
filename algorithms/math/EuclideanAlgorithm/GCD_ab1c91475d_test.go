// Test generated by RoostGPT for test math-go using AI Type Open AI and AI Model gpt-4

package EuclideanAlgorithm

import "testing"

func TestGCD_ab1c91475d(t *testing.T) {
    testCases := []struct {
        a, b, expected int
    }{
        {10, 5, 5},
        {14, 28, 14},
        {100, 200, 100},
        {3, 7, 1},
        {0, 5, 5},
        {5, 0, 5},
        {0, 0, 0},
    }

    for _, testCase := range testCases {
        result := GCD_ab1c91475d(testCase.a, testCase.b)
        if result != testCase.expected {
            t.Errorf("GCD of (%d, %d) was incorrect, got: %d, want: %d.", testCase.a, testCase.b, result, testCase.expected)
        } else {
            t.Logf("GCD of (%d, %d) was correct, got: %d.", testCase.a, testCase.b, result)
        }
    }
}

func GCD_ab1c91475d(a, b int) int {
    for b != 0 {
        t := b
        b = a % b
        a = t
    }
    return a
}
